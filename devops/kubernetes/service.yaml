apiVersion: v1
kind: Service
metadata:
  name: user-management
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name : http
  selector:
    app: user-management
  type: LoadBalancer
